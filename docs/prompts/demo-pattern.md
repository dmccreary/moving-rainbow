The one-pixel per click is fine.  The users can precisely control the position on the strip.  Now I would like us to work on a more formal "21-demo.py" program that is a fun demonstration of the capability of the box.  This will be a larger program but similar to the 20-mode-machine.  However, it will have ALL of the sample patterns that we discussion in the book including things like the larson scanner and the candle flickering patterns.  There are also several patterns that use the random number generator to generate colors - but in a way that the colors are distinct to the human eyel. There is one characteristic of each of the modes.  When the students spin the rotary encoder back and forth they MUST see a change in each pattern.  It might be the position of a pixels or the initial position of a pattern.  This demo pattern will be the new default main (after we get it working).  Step 1, please propose a list of patterns and verify that we can see a change upon spinning the rotary encorder.  Step 2 generate a new 21-demo.py code for me to test.

This box is used as a "demo of capability" in many events such as science fairs, STEM events, and teacher education.  We have a table of these boxes and the teachers/students and parents come by and pick up the project boxes and play with them.  We usually have only about 60-seconds to explain each box and get the participants of the events "hooked" on how fun these boxes are and how they can teach computational thinking while promoting low-cost fun project that feature lights, color and motion.  Note this in the README.md file.